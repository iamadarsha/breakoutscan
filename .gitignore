.DS_Store
.env
.env.*
!.env.example
!.env.*.example
.venv
node_modules
.next
.expo
dist
build
coverage
.pytest_cache
__pycache__
*.pyc

# Sensitive project handoff files (contain API keys)
PROJECT_HANDOFF.md
PROJECT_HANDOFF_HIDDEN_KEYS.md

# Local-only worksheet for filling in real rotated credentials — never
# holds a value until the user manually edits it, and must never be
# committed even then.
CREDENTIAL_ROTATION_WORKSHEET.md

.claude/worktrees/
.venv-phase3/
.claude-flow/
*.tsbuildinfo

# Vercel CLI link metadata
.vercel
